# Маркер корня пакета (web/ — независимый пакет, без workspaces — решение №3)
# + разрешение build-скриптов (pnpm блокирует postinstall по умолчанию).
allowBuilds:
  # телеметрия-аналитика из транзитивных зависимостей orval — скрипт не нужен
  '@scarf/scarf': false
  esbuild: true
