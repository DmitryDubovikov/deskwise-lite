{
	"name": "api",
	"version": "1.0.0",
	"scripts": {
		"dev": "tsx watch src/index.ts",
		"test": "vitest run",
		"lint": "biome check .",
		"typecheck": "tsc --noEmit",
		"check": "pnpm lint && pnpm typecheck && pnpm test"
	},
	"license": "ISC",
	"type": "module",
	"dependencies": {
		"fastify": "^5.10.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.4",
		"@types/node": "^26.1.1",
		"tsx": "^4.23.1",
		"typescript": "^7.0.2",
		"vitest": "^4.1.10"
	}
}
